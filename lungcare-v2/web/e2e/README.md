# End-to-end tests

The Playwright runner is configured, but browser binaries and end-to-end scenarios are intentionally deferred until the shared persistence boundary is implemented. The future suite must use separate Patient, Nurse, and Doctor sessions.
